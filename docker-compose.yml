version: '3.1'

services:

  db:
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_PASSWORD: qwertys17
      POSTGRES_USER: docker_db_user
      POSTGRES_DB: docker_db
    restart: always
